{
  "name": "assetcare-cap-training",
  "private": true,
  "description": "SAP CAP guided project (AssetCare) - course tooling (Jest grader + CAP runtime)",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@sap/cds": "^9.9.1",
    "@cap-js/sqlite": "^2.4.0",
    "@cap-js/postgres": "^2.3.0"
  },
  "devDependencies": {
    "@sap/cds-dk": "^9.9.2",
    "@cap-js/cds-test": "^1.0.0",
    "@types/jest": "^29.2.5",
    "jest": "^29.3.1"
  },
  "scripts": {
    "test": "jest",
    "start": "cds-serve",
    "watch": "cds watch"
  }
}
