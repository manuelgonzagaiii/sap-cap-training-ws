{
  "requires": {
    "db": {
      "[development]": {
        "kind": [[PH:the development database kind - "sqlite" for the fast local loop]]"sqlite"[[/PH]],
        "credentials": { "url": [[PH:the SQLite database file, e.g. "db.sqlite"]]"db.sqlite"[[/PH]] }
      }
    }
  }
}
